{
  "name": "runner-dashboard-site",
  "private": true,
  "version": "1.0.0",
  "description": "Runner Dashboard static app with Vercel API routes and Supabase sync.",
  "scripts": {
    "dev": "http-server -p 8000 public",
    "dev:vercel": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "vercel:link": "vercel link",
    "vercel:env:pull": "vercel env pull .env.local",
    "vercel:dev": "vercel dev",
    "vercel:build": "vercel build",
    "vercel:deploy": "vercel deploy --prod",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {
    "@supabase/server": "^1.4.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@google/gemini-cli": "^0.52.0",
    "http-server": "^14.1.1",
    "vercel": "^41.7.3",
    "wrangler": "^4.26.0"
  }
}
